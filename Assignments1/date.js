let day=12;//parseInt(prompt("Enter a number between "));
let month=3;//parseInt(prompt("Enter a month"));
if((month >= 3  && month<=6)&(day <=31)){
    console.log("month valid");
}
else{
    console.log("not valid");
}